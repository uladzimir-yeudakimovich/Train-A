import { DatePipe } from '@angular/common';
import { MatButton } from '@angular/material/button';
import {
  MatDialogActions,
  MatDialogClose,
  MatDialogContent,
  MatDialogTitle,
} from '@angular/material/dialog';

export const orderDialogImports = [
  MatDialogActions,
  MatDialogContent,
  DatePipe,
  MatButton,
  MatDialogClose,
  MatDialogTitle,
];
