import { JoinWithDashPipe } from './join-with-dash.pipe';

describe('JoinWithDashPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinWithDashPipe();
    expect(pipe).toBeTruthy();
  });
});
